# Incident Runbook
Triage with `kubectl get nodes`, `kubectl get pods -A`, and application logs. Check Grafana, Prometheus, CloudWatch and X-Ray. Record detection, acknowledgement, mitigation and recovery. Use Project 2 Helm/GitOps rollback for release regressions. Confirm SLO recovery and document RCA/error-budget impact.
